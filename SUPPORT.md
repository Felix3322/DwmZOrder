# 支持范围

特征模型在 DWM 已加载的 `uDWM.dll` 内按函数边界解析，并校验窗口数据布局、虚表槽、CFG 元数据、链表和视觉树方向。任一依赖无法唯一解析都会拒绝修改，不会使用旧版本固定地址。

| 系统 / 架构 | 已审查组件版本 | 状态 |
| --- | --- | --- |
| Windows 10，19041 组件系列，x64 | `19041.546`、`19041.6157`、`19041.6456` | 支持 |
| Windows 11 24H2，x64 | `26100.1`、`26100.1591`、`26100.2454`、`26100.3037`、`26100.4061`、`26100.4343`、`26100.5074`、`26100.7705`、`26100.7920`、`26100.9022`、`26100.9278` | 支持 |
| 其它 Windows 版本、x86 / ARM64 | 未建立完整模型 | 暂不支持 |

这里的版本号指 `uDWM.dll` 文件版本，不是 `winver` 显示的系统构建号。文件模型匹配只证明离线解析成功，不等于每个版本都完成实际桌面注入和视觉验收。跨 UIAccess、系统界面、多显示器、动画、最小化和安全桌面仍需单独观察。

系统更新后如果 DWM 尚未重启，磁盘上的 DLL 可能与已加载版本不同。更换已经加载的代理版本后，需要注销并重新登录；不能在同一个 DWM 进程中热替换 DLL。
